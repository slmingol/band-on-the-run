services:
  band-on-the-run-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
      - ./public/audio/stems:/app/public/audio/stems:ro
    environment:
      - VITE_HMR_HOST=localhost
